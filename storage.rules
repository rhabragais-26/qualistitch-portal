rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to the Carousel folder for any authenticated user.
    match /Carousel/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
